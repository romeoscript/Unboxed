{
  "name": "product-parser-api",
  "version": "1.0.0",
  "description": "API that extracts structured product data from URLs using OpenAI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "openai": "^4.19.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}